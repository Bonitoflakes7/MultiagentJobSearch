# Copy to .env for local development. Never commit real secrets.
JOB_SEARCH_ENV=development
JOB_SEARCH_DATA_DIR=data
JOB_SEARCH_MAX_RETRIES=3
JOB_SEARCH_ALLOWED_ACTIONS=export_resume
# Configure a model provider only when enabling LLM-backed CrewAI tasks.
# OPENAI_API_KEY=

